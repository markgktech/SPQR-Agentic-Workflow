---
SPQR spqr.config template — init-only
Copy this file to `spqr.config` in your project root and fill every value before first propagation.
Each key is a config token from docs/CONFIGURE.md — the authoritative catalogue this map is derived from.
The propagation agent reads these key→value pairs to re-instantiate the matching [TOKEN] inline in core files.
Any token a propagated core file uses must exist here, or propagation fails loudly.
Runtime placeholders ([AGENT], [TICKET_URL], [FILE], [ID]) are NOT config — they are filled per-session and must not appear here.
---

# Generic version this project is currently stamped at.
# Set the baseline by hand at first instantiation; thereafter the propagation
# agent writes it at the atomic commit point of each bring-current run.
version: ""

# Senate / Quaestor personas — docs/CONFIGURE.md §1
Name 1: ""
Name 2: ""
Name 3: ""
Name 4: ""

# Project — docs/CONFIGURE.md §1
PROJECT_PATH: ""
PROJECT_BOUNDARIES: ""            # ticket-slicing rules; empty = silent failure

# Knowledge Warehouse — docs/CONFIGURE.md §1
WAREHOUSE_ROOT: ""               # warehouse root passed to the warehouse_robot CLI; required, empty = silent failure
ANTECHAMBER_ROOT: ""             # optional; empty = robot defaults to an `antechamber` sibling of WAREHOUSE_ROOT

# Notion ticket / spike-doc IDs — docs/CONFIGURE.md §1–2
SPIKE_TEMPLATE_ID: ""
FEATURE_TEMPLATE_ID: ""
BUG_TEMPLATE_ID: ""
DOC_TEMPLATE_ID: ""
SPIKE_DOCUMENT_TEMPLATE_ID: ""
SPIKE_DOC_PARENT_PAGE_ID: ""

# Retro — docs/CONFIGURE.md §1
# All retro tokens removed in v1.5 (SAW-46): pass 1 dropped RETRO_PARENT_ID / RETRO_TEMPLATE_ID
# (RETROACTIO went local, SAW-31/33); the second pass (Item 4) dropped RETRO_SESSION_STARTER_ID —
# retro is now fully local, Notion retained only for ticket creation/maintenance, so no retro
# Notion mirror remains. No retro config slots.
