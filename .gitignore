.DS_Store
**/.DS_Store
Thumbs.db

# Secrets — never commit (public repo)
.mcp-config.json
*.xcconfig
.env
.env.*
*.pem
*.key
*.p8
*.p12

## Obsidian file not needed
.obsidian

# Warehouse derived index — disposable projection of the markdown truth (S7); never versioned
index.sqlite
index.sqlite-wal
index.sqlite-shm
__pycache__/