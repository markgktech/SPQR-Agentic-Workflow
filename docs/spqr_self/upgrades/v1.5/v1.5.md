---
run: v1.5
title: "v1.5 — run index"
status: partial             # planning | in-progress | done | partial
# partial: SAW-38/SAW-32/SAW-33 complete; SAW-29 (CORRECTIO bug pipeline) complete — group 4, 9 files, master-verified; SAW-26 receipt rule complete — group 5; SAW-27 detection-health sensors complete — group 6, 3 files, master-verified; warehouse workstream folded in as groups 7–8 (spec & decisions + build B1–B5): B1–B3 done/green, B4–B5 + independent Phase 1 exit check pending.
target_version: v1.5
covered_saw: [SAW-38, SAW-32, SAW-33, SAW-29, SAW-26, SAW-27, SAW-30]
poc: ["[[SPQR_Propagation_Mechanism_PoC]]", "[[Git Branching Strategy — PoC]]", "[[SAW-33 FDP Ticketing Alignment — PoC]]", "[[SAW-29 Bug Pipeline — PoC]]", "[[SAW-27 Detection-Health Sensors — PoC]]", "[[Knowledge Architecture — Pre-Build Decision List (Planning Session Input)]]", "[[Knowledge Architecture & Token Optimization — Direction Checkpoint PoC]]"]
date_opened: 2026-06-17
tags: [run]
---

# v1.5 — run index

## Summary
Index folder-note for the v1.5 run. v1.5 carries more than one workstream; this note is the spine that indexes them. Currently active: the **SPQR Propagation Mechanism** (SAW-38) — how generic SPQR updates reach consuming projects. The Knowledge Warehouse Initiation Project is now folded in as groups 7 (spec & decisions) and 8 (build, B1–B5); its files live in this folder.

## Why now
- The generic SPQR moved ahead; consuming projects (Foodoire, currently on v1.3) lag with no defined "bring-current" path. SAW-38 settles the direction and mechanism.

## Covered SAW tickets
- SAW-38 — Propagate generic SPQR updates to consuming projects (Foodoire) — direction + mechanism.
- SAW-32 — Git Branching Strategy — adopt PoC into SPQR process & agent mandates (GitHub Flow, single git-mechanics source, weed old worktree refs).
- SAW-33 — Align generic SPQR with the DOC-21 / FDP ticketing rework — re-point agent/skill/retro definitions from Notion-comment work-trace to the local hub+output+handover model.
- SAW-29 — Bug-pipeline: define the lean, distinct bug-type ticket flow (CORRECTIO) reusing existing primitives; resolves the SAW-33/D15 "BUG roles deferred".
- SAW-26 — Receipt rule: bind every build/test/lint claim to verbatim tool evidence (producer Praetor/Probator + enforcer Censura + shared handover protocol); cost-guard floor (C6).
- SAW-27 — Detection-health telemetry sensors: deterministic detection counters DERIVED from the existing record (verdict color+round · revision rounds · where-caught · recurring via a Censura failure-category enum), harvested at retro time (no standing store), with the retro re-pointed as reader (left-shift + balance metric); metric reframe documented. Cost stays in the hub Session/cost table. Thin now-wedge under the SAW-24 observability epic.
- SAW-30 — Knowledge Warehouse Initiation Project: build the warehouse robot in the generic SPQR repo (B1 store/DDL · B2 fold · B3 query · B4 write gate · B5 audit) with versioned fixtures, then import to Foodoire, migrate, and cut SPQR over to the warehouse (v1.5). Build phase B1–B3 done/green; B4–B5 + independent Phase 1 exit check pending (groups 7–8).

## Implementation groups
| # | Group | Order | Sub-doc | Status |
|---|-------|-------|---------|--------|
| 1 | Generic-side propagation mechanism | 1/1 | [[01-propagation-mechanism]] | done |
| 2 | Git workflow single-source + session wiring | 2/2 | [[02-git-branching-strategy]] | done |
| 3 | FDP Ticketing Alignment (SAW-33) | 3/3 | [[03-fdp-ticketing-alignment]] | done |
| 4 | CORRECTIO bug pipeline (SAW-29) | 4/4 | [[04-bug-pipeline]] | done |
| 5 | Receipt rule — verbatim build/test/lint evidence (SAW-26) | 5/5 | [[05-receipt-rule]] | done |
| 6 | Detection-health telemetry sensors (SAW-27) | 6/6 | [[06-detection-health-sensors]] | done |
| 7 | Warehouse Initiation — spec & decisions | 7/8 | [[07-Execution Plan — Warehouse Initiation Project]] · [[07-Planning Decisions — Warehouse Initiation Project]] · [[07-Session Starters — Build Tickets & Exit Check]] | done |
| 8 | Warehouse Initiation — build (B1–B5) | 8/8 | [[08-B1 Delivery Note — Markdown Store, Node-Edge Layout & SQLite DDL]] · [[08-B2 Delivery Note — The Fold: Incremental Upsert, Reconcile Rebuild & Divergence Check]] · [[08-B3 Delivery Note — Query Interface: Verbs, Bracket, Budget & DENY]] · [[08-roundtable-b4-b5-exit]] · [[08-b4-write-gate]] | partial — B1–B3 done/green; B4 brief ready (Phase 4); B4 build + B5 + exit pending |

> Groups 1–6 are independent workstreams folded into the same v1.5 run; decisions live in each group's PoC (this index links, does not inline). Groups 7–8 are the Warehouse Initiation Project — group 7 its spec/decision layer (Execution Plan + Planning Decisions + Session Starters), group 8 its build deliverables (B1–B5 delivery notes); warehouse decisions live in the Execution Plan + Planning Decisions and the warehouse PoC (linked, not inlined).

## Decisions
<!-- #decision — tag cross-run, durable decisions so they stay findable -->
Propagation-mechanism decisions (Q1–Q7 + amendments AM1–AM6) are recorded in the PoC: [[SPQR_Propagation_Mechanism_PoC]]. Summary: directory-level declarative manifest; full core-version snapshot; agent-driven (no deterministic code now); generic→project one-way content flow; per-project `spqr.config` derived from CONFIGURE.md.

FDP Ticketing Alignment decisions (D1–D15) are recorded in the PoC: [[SAW-33 FDP Ticketing Alignment — PoC]]. Summary: transport swap Notion-comment → local `<TICKET-ID>_handover.md` (`---`-block append, ticket-comment field contract preserved); impl-doc → local `<TICKET-ID>_output.md` (NOTES removed); revisions → `_output_revN.md`; executor creates the hub (Praetor DEV, Quaestor SPIKE/DOC) + session/cost table (owner fills cost via `/usage`); `<TICKET-ID>` placeholder (FDP-N example); scoped Write/Edit added to every agent with guardrails (never modify SPQR process files/CLAUDE.md, never delete, append-only, reviewers never write code); Notion kept only for ticket definition/creation; retro fully aligned + input re-pointed to local handover. 40 files edited (1 agent, 4 passes); master validation applied 4 fixes (canonical tags `content/spike` + `content/doc-change`; retro template → local). All 5 execution flags adjudicated.

Git Branching Strategy decisions (D1–D13) are recorded in [[02-git-branching-strategy]]. Summary: the core fix is structural — collapse scattered/diverged git guidance into one named source (`docs/skills/git-workflow.md`) that the agent sessions reference, and weed the old worktree copies (`praetor.md`, `session-starters.md`). Content = GitHub Flow (PoC). Mechanics vs policy split (mechanics in the skill; owner-only-commit/merge policy referenced from senate.md + CLAUDE.md.template). I12 (Foodoire `## Branching`) descoped to post-1.5 owner action; "no `censura.md` mandate" flagged as an open process-debt item.

CORRECTIO bug-pipeline decisions (D1–D27) are recorded in the PoC: [[SAW-29 Bug Pipeline — PoC]]. Summary: a lean, distinct bug flow (**CORRECTIO**) — default **2 sessions** (Praetor investigate-first + HITL cause-note gate + fix → Probator verify+close); routing on the two hub axes (`entry: wild|dev-linked` + `severity: HIGH/MED/LOW`); Consilium/Tribunus-review/Curator cut by default; **Censura conditional + decision-triggered** (knowledge-base expansion, not a quality gate); mechanical escalation only (investigator for unlocalizable wild bugs; Tribunus-review/Curator for HIGH/critical surface); resolves SAW-33/D15. Generic-side only this run (**G4**, CORE surface, propagates); Foodoire-owned template edits flagged downstream. **9-file single-agent run** (7-FILES cap owner-overridden; planning audit added `tribunus.md` + `curator.md` enumeration fixes); bug-flow veto/revision REUSES the existing mechanic (D26); Quaestor escalation = a normal spike ticket, `quaestor.md` unchanged (D12). Model-tier pinning (D10) descoped.

Detection-health sensor decisions (D1–D8 + owner calls A/C/D) are recorded in the PoC: [[SAW-27 Detection-Health Sensors — PoC]]. Summary: 4 detection counters DERIVED from the append-only handover (verdict color+round, revision rounds, where-caught inferred-not-self-reported, recurring keyed on a new minimal Censura failure-category enum); files_loaded dropped (cost → hub Session/cost table, manual /usage); harvest runs AT retro time, NO standing store (residue guard), trend persists in the retro output lineage; the quantitative-telemetry ban is reframed (not deleted) in all 4 locations so derived counters are in-scope while a standing store stays out; retro gains a harvest+interpretation step (counterfactual guard = same-terminal-stage comparison; balance metric escape-to-owner v1 = in-record half; metric reframe read-side; trend+narrative not threshold); generic SPQR only (project-side retro template C + ticket-text D8 via owner/propagation). 3 generic files (retro/input.md, retro/retrospector.md, censura-output.md).

Receipt-rule decisions (D1–D8) are recorded in the run sub-doc (lean run — no separate PoC): [[05-receipt-rule]]. Summary: producer/enforcer split (Praetor=build+lint, Probator=test produce; Censura enforces — Senate runs no shell); one canonical `receipt:` field in `ticket-comment.md`; scoped to the decisive stdout line (not full log), whitelist build/test/lint; cost-guard C6 (never skipped to save tokens); verbatim home = Praetor `_output.md` VERIFICATION + compact handover line, Probator decisive line in handover; missing receipt = HITL flag + cheap producer bounce (NOT a standalone veto — real failures use existing veto/RED path); generic SPQR only (Foodoire via SAW-38 propagation, separate); enforcement at Censura only (scope-faithful). 7 generic skill files. Adjacent residue found: R1 below.

Warehouse Initiation (build B1–B5) decisions (G1–G8 + amendments A1–A18) are recorded in [[07-Planning Decisions — Warehouse Initiation Project]] (governed by [[07-Execution Plan — Warehouse Initiation Project]]) and the warehouse PoC; the B4/B5 roundtable findings live in [[08-roundtable-b4-b5-exit]]. Summary: per-call CLI robot (G1/G2); robot writes files, never commits (G3); generic-first build then plain-copy import to Foodoire (A1/A2); test isolation directory-level not git-level (A4); Phase 1 exit = two-part byte/digest reconcile check (A8). **B4/B5 closure decisions (A9–A18):** Starter-A-governed + augment-not-replace (A9); 3-layer verification incl. master critical re-test (A10); verbatim receipt + ≥5× + SQLite version, lint out (A11); test-isolation hardening incl. antechamber (A12); canonical antechamber versioned not gitignored (A13); B5 tripwire shapes + K=5 placeholder + lesson→about seed (A14); B4 design positions (A15); fixture growth (A16); e2e layering L1+L2 generic / L3 Foodoire (A17); roadmap order + flat-docs-stay + named deferrals (A18). B1–B3 delivered green; B4 (write gate) + B5 (audit tripwires) + independent Phase 1 exit check pending.

## Stalled / Deferred
- **SAW-31 — SPQR cutover to the warehouse** (forward reference, NOT this run's delivery; kept out of `covered_saw`): the separate "SPQR upgrade to handle warehouse" ticket — re-points agent/skill files to consume the warehouse (per-archetype query-policy blocks, ingest skill, antechamber + revise-wake path). **Gated on SAW-30 warehouse testing** (Phase 1 exit check + migration) going green; sequenced after, tracked on its own ticket.
- **(SAW-26 / R1) "12 line" handover cap was Notion-era residue — RESOLVED.** `ticket-comment.md:2` + `:29` reframed to a brevity principle (Option A); reason (Notion comment size) was removed by the SAW-33 transport swap but the cap survived via "preserve field contract verbatim". `retrospector.md:26` mention reconciled. **R2 (LOW, same pass):** `censura-input.md` LOAD ORDER now loads `ticket-comment.md` (enforcer referenced the canonical receipt def without loading it). → Filed retroactively as **SAW-39** (covers R1 + R2) for traceability.
- **Process-finding** (multi-group version container handling + decision-anchor + legacy format + missing `## Decisions` in `group_submd_template`) → new SAW ticket (owner to create).
- **Outsource propagation to deterministic code** later (AM4 trade-off) → candidate SAW ticket (owner to create).
- **`warehouse_robot/` propagation classification** → decided at the warehouse stage, not here.
- **(SAW-32) No `docs/agents/censura.md` mandate file** — Censura has skill files but no mandate; latent process-debt, independent of git. Candidate ticket under the SAW-25 (Operations / process-debt) epic → owner to create.
- **(SAW-32, D13) Foodoire `CLAUDE.md` `## Branching`** is the old worktree/milestone model and must be **replaced** with the GitHub Flow policy → **descoped to a post-1.5 owner action** (no active Foodoire development now).
- **(SAW-32, D12) Pending owner Notion actions** — SAW-32 `Stage` → Done and `Target` path-drift fix (`docs/specifications/poc/…` → `docs/spqr_self/poc/…`); the master flags, does not edit ticket properties.
- **(SAW-33) Process-debt — PoC-first step** — the upgrade pipeline does not prescribe "decisions → PoC → execution brief" → candidate SAW ticket (owner to create).
- **(SAW-33) Brief 7-FILES cap** — does not fit a homogeneous single-surface sweep; owner decided to KEEP it as-is and override case-by-case when a single coherent surface warrants one agent (as in this run). **No ticket.**
- **(SAW-33) CONFIGURE.md token-catalogue reconciliation** — still documents the old token set (`DEV-XXX`, `*_TEMPLATE_ID`s, parent-page IDs, Notion-comment assumptions); SAW-38 propagation derives `spqr.config` from it, so a stale catalogue would propagate wrong tokens → candidate SAW ticket (owner to create).
- **(SAW-33) POC & BUG handover-chain roles** — structure stands; role assignment owner-deferred (TBD), per DOC-21. → **BUG part now RESOLVED by SAW-29 (CORRECTIO, group 4)**; POC roles still deferred.
- **(SAW-29) Foodoire-owned bug-template follow-up** — apply the CORRECTIO chain to Foodoire's `handover_template.md` ("BUG: TBD" → chain), align `bug_output_template.md`, create `dev_logs/correctio_outputs/`. Project-owned (not propagated); owner-run against Foodoire with/after the v1.3→v1.5 propagation → candidate SAW ticket (owner to create).
- **(SAW-29, D10) Bug model/effort tiers** — descoped; owner sets per bug at runtime. Reopenable if a fixed routing is wanted.
- **(SAW-29, D24) Validation** — dry-run-map the 5 FDP bugs onto CORRECTIO once group 4 lands (proves routability without touching Foodoire).
- **(SAW-27, C) Retro template trend-section** — the sink that fully closes the reader-loop (persists the harvested trend across runs). `retro/output.md` forbids ad-hoc template additions, so it is a project-side `retro_template.md` edit reconciled generic→project via propagation. Generic side (harvest + interpretation) is done; full end-to-end closure waits on this. → owner/project action (candidate SAW ticket).
- **(SAW-27, A) Post-close-defect tracking** — the post-close half of the balance metric (escape-to-owner). Ride on CORRECTIO (SAW-29): link a bug to its originating ticket; the retro counts bugs tracing to recently-closed tickets. Needs a CORRECTIO-link convention → candidate SAW ticket.
- **(SAW-27, D7/D) Enum-vocabulary curation** — long-term governance of the failure-category enum as new modes appear; first real review at the next retro once LESSONS accrues entries (currently header-only). The enum was seeded from the PoC D7 set, not live data. Handled in-loop via the retro flag-candidate step; candidate SAW ticket only if formal curation is wanted.
- **(SAW-27, D8) Notion ticket-text transport-wording fix — RESOLVED this session.** SAW-27 + SAW-24 `Target` and SAW-27 body re-pointed from "ticket-comment template / Notion comment" to the local `<TICKET-ID>_handover.md` (ticket-comment.md field contract) + retrospector harvest + hub Session/cost surfaces. Owner-authorized inline fix.
