---

---
> ⚠ **Filename correction (resolved in Group 3):** "[quaestor-input.md](http://quaestor-input.md/)" everywhere in this brief means `**quaestor-relatio.md**` — the Quaestor pre-flight skill (its PRE-FLIGHT / "Load in order" block). There is **no **`**quaestor-input.md**` in Foodoire (DOC-14 ticketing slip). The FETCH STRATEGY was added after that PRE-FLIGHT block. **DOC-14 shares **`**quaestor-relatio.md**`** with Group 2/DOC-16** (different section — verify block in the research mandate; no collision because sequential + read-current). The SPQR-sync group should target `quaestor-relatio.md`, not `quaestor-input.md`.

Make the running pipeline cheaper / more traceable — non-functional improvements. **Foodoire-first → SPQR sync.**

---

## DOC-14 — Fetch strategy (narrowed)

[Ticket](https://www.notion.so/37168d5de1e88109817dd63823cf6ce9)

**Decided direction:** narrowed to **cheap, model-agnostic fetch hygiene only**, `quaestor-input.md` only; apply-everywhere **deferred** (premature without telemetry). Structural cost-fix (knowledge files scale without linear token cost) → [SAW-13](https://www.notion.so/37368d5de1e881c3b77aeb97d9af0d0e).

## DOC-17 — session_id

[Ticket](https://www.notion.so/37268d5de1e881679910f301df337223)

**Decided direction:** add `session_id` to `ticket-comment.md`; trivial, candidate matches the problem. = observability primitive → feeds telemetry (roadmap 2).

---

## Execution brief — Operational Quality (Group 3)

*Self-contained (v1.2 form) — the execution agent can act from this page alone.*

**Metadata**

- **GROUP:** Operational Quality · **ORDER:** Foodoire, collision-free (DOC-14 narrowed to one file — no "last" requirement); SPQR-sync last (finalize N/N when all briefs exist)
- **REPO:** Foodoire (SPQR placeholder versions → sync group)
- **RATIONALE:** two small non-functional fixes to distinct skill files; no shared files
- **READ_CURRENT_STATE:** yes — read each file before editing · **FILL_CHANGES_MADE:** yes

**PRE_FLIGHT (load these):**

- DOC-14 ticket + DOC-17 ticket (background)
- `quaestor-input.md` — read the current LOAD ORDER section (anchor; confirm it exists)
- `ticket-comment.md` — read the current FORMAT section (anchor; confirm it exists)

**Affected files (Foodoire):** `docs/skills/quaestor-input.md` (UPDATE) · `docs/skills/ticket-comment.md` (UPDATE). Distinct files — no collision.

### `docs/skills/quaestor-input.md` — UPDATE (DOC-14)

**What:** Add a **FETCH STRATEGY** section after the existing LOAD ORDER section — **only the 2 model-agnostic rules:** (1) **verification-skip** — when a Notion create/write returns success + URL, treat it as confirmed; do not re-fetch to verify a **routine** write — **BUT do verify after a structurally complex / format-sensitive write** (e.g. multi-section content, code blocks); (2) **WebFetch no-go** — external URLs only; skip Notion-internal links, GitHub-private repos, App Store URLs (403 expected). **NOT applied to other skill files** — deferred (premature without telemetry; revisit when telemetry shows where cost concentrates). The original ticket's partial-read heuristics are also deferred (model-dependent — a strong model judges partial vs. full itself).

**Where:** `quaestor-input.md` — new FETCH STRATEGY section after LOAD ORDER.

### `docs/skills/ticket-comment.md` — UPDATE (DOC-17)

**What:** Add `session_id` to the comment **FORMAT** section: `session_id: [claude_cli_conversation_id]`. Value retrieved via `echo $CLAUDE_CODE_SESSION_ID` (Bash) at comment-writing time. **Field rule:** always present — the agent fills in its own session ID. **NEVER omit** the field. (If the env var is somehow unset, record `unknown` rather than dropping the field.)

**Where:** `ticket-comment.md` — FORMAT section + a NEVER constraint.

**Out of scope here:** DOC-14 structural cost-fix (knowledge files scale) → SAW-13 · broad fetch-discipline → deferred (post-telemetry) · DOC-17 `session_id` feeds telemetry (roadmap 2).

### Changes Made (Group 3)

`**docs/skills/ticket-comment.md**`** (DOC-17) — DONE**

- FORMAT: added `session_id: [claude_cli_conversation_id]` as first field.
- FIELD RULES: added session_id rule — always present; value via `echo $CLAUDE_CODE_SESSION_ID` at comment-writing time; if env var unset record `unknown`, never drop the field.
- CONSTRAINTS: added `Never omit session_id`.

`**docs/skills/quaestor-relatio.md**`** (DOC-14) — DONE (target corrected)**

- Added a **FETCH STRATEGY** section directly after the PRE-FLIGHT block, with only the 2 model-agnostic rules: (1) verification-skip (success+URL → confirmed; skip re-fetch on routine writes, but DO verify after structurally complex / format-sensitive writes); (2) WebFetch no-go (external URLs only; skip Notion-internal / GitHub-private / App Store).
- Deferred as instructed: partial-read heuristics (model-dependent) and apply-to-all-skill-files (post-telemetry). Structural cost-fix → SAW-13.

**Target discrepancy (flagged + resolved with owner):** brief + DOC-14 named `quaestor-input.md` / `LOAD ORDER` section, which does not exist in Foodoire (git history confirms it never did). Quaestor pre-flight skill was restructured into `quaestor-relatio.md` (`PRE-FLIGHT` block = `LOAD ORDER` equivalent). Owner confirmed relatio = the quaestor input (ticketing slip). SPQR generic/template copy intentionally NOT touched — separate sync group.

**Repo:** Foodoire only. **No git commit/push. **[**CLAUDE.md**](http://claude.md/)** untouched.**

**Execution session_id:** `6ce70275-12e4-46d8-8feb-571ca8ba9a2c`