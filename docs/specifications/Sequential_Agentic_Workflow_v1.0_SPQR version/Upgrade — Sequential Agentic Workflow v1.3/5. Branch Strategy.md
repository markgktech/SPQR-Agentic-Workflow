---

---
Make branch granularity follow DELIVERABLE granularity, and remove the hard-coded per-ticket worktree assumption. Foodoire-first → SPQR sync. Branch decision owned by Consilium (writes the ticket Branch property); Praetor consumes it.

---

## Execution brief — Branch Strategy

Self-contained (v1.2 form) — the execution agent can act from this page alone.

Metadata

- GROUP: Branch Strategy · ORDER: Foodoire first; SPQR-sync last
- REPO: Foodoire (SPQR placeholder versions → sync group)
- RATIONALE: the hard-coded worktree line cannot aggregate a milestone bundle onto one branch; the ticket Branch property is the existing-but-unused mechanism
- READ_CURRENT_STATE: yes — read each file before editing · FILL_CHANGES_MADE: yes

PRE_FLIGHT (load these):

- SAW ticket (this) for background
- docs/agents/session-starters.md — read the current worktree line (anchor; confirm it exists)
- docs/agents/praetor.md — read the worktree/isolation + handoff sections (anchor)
- START IN: ../milestone-0-foundation worktree — NOT main, and do NOT create a new branch for this ticket (self-reference: this agent fixes branch logic but still starts under the old logic; owner points it to the milestone worktree manually)

Affected files (Foodoire): docs/agents/session-starters.md (UPDATE) · docs/agents/praetor.md (UPDATE). CLAUDE.md branching-policy = OWNER action, not agent.

## docs/agents/session-starters.md — UPDATE

What: Replace the hard-coded worktree line with Branch-property-driven setup.

```shell
# was: git worktree add ../TICKET-XXX-branch TICKET-XXX-branch
BRANCH = ticket.Branch          # set by Consilium (owner-approved)
if BRANCH is empty:            ASK owner — never default to main
elif branch BRANCH exists:
    if a worktree for BRANCH exists:  cd into it (attach)
    else:                             git worktree add ../BRANCH BRANCH
else:                          git worktree add ../BRANCH -b BRANCH
```

Where: the worktree line in session-starters.md.

## docs/agents/praetor.md — UPDATE (teardown gate)

What: Before any worktree removal, require committed/stashed state. Rule: never --force-remove a worktree with uncommitted or untracked changes — commit or stash first.

Where: worktree/isolation + handoff/teardown section.

## Consilium integration

The Consilium decides branch scope (single ticket vs milestone bundle) with owner approval, and writes the ticket Branch property. The Praetor READS it — it is not manual session-starter input. Empty Branch → Praetor asks; foundation/milestone bundle → shared branch with direct commits; feature → per-ticket branch → merge.

## CLAUDE.md — OWNER ACTION (not agent)

Add a ## Branching section. Owner applies it on the milestone-0-foundation branch (agents never write CLAUDE.md). Exact text supplied separately by owner.

## SPQR template sync

Apply the same changes to the SPQR template repo in placeholder form. Separate repo: owner commits now, releases with v1.3 later.

## Out of scope

- Migrating existing branches/worktrees (manual one-off)
- Notion DB schema change (Branch property already exists)
- CLAUDE.md edit by the agent (owner action)

## Changes Made

**Docs/agents/**[**session-starters.md**](http://session-starters.md/) — PRAETOR PRE-STEP rewritten. Replaced hard-coded `git worktree add ../TICKET-XXX-branch TICKET-XXX-branch` with Branch-property-driven logic: reads `ticket.Branch` (set by Consilium); empty → asks owner (never defaults to main); existing worktree → attach (cd into it); existing branch, no worktree → `git worktree add ../BRANCH BRANCH`; new branch → `git worktree add ../BRANCH -b BRANCH`.

**Docs/agents/**[**praetor.md**](http://praetor.md/) — Added WORKTREE TEARDOWN section directly after the Isolation line in ALLOWED TOOLS. Rule: verify committed and untracked-clean state before any worktree removal; never --force-remove with uncommitted or untracked changes — commit or stash first.

[CLAUDE.md](http://claude.md/) — not touched (owner action). SPQR template sync — not in scope for this session.