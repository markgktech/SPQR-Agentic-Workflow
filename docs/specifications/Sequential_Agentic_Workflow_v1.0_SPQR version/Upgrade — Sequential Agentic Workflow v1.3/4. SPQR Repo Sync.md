---

---
Sync all v1.3 Foodoire changes to the **generic SPQR template repo** with substitutions. **Runs LAST** (depends on Groups 1–3). The execution agent runs **in the SPQR template repo**.

---

## Execution brief — SPQR Repo Sync (Group 4)

*Self-contained (v1.2 form). Mechanical copy + substitute; runs after Groups 1–3 are done.*

**Metadata**

- **GROUP:** SPQR Repo Sync · **ORDER:** LAST (after Groups 1–3)
- **REPO:** SPQR template — `/Users/kovacsmark/Documents/GitHub/Marks-agentic-workflow-SPQR`
- **RATIONALE:** one mechanical sync — all v1.3 changes → generic repo with substitutions; verified together
- **READ_CURRENT_STATE:** yes · **FILL_CHANGES_MADE:** yes

**Source of truth:** the **Foodoire v1.3-edited files** — read them and replicate generically. Foodoire repo: `/Users/kovacsmark/Documents/RecipeAPP/Foodoire`.

**Substitution rules (v1.2 precedent):**

- persona / real names → `[Name 1]`–`[Name 4]` (match the placeholder convention already in the template)
- iOS / SwiftData / project-specific content → `[PROJECT_BOUNDARIES]`
- Notion IDs / URLs → named placeholders; the 3 new retro IDs → `[RETRO_PARENT_ID]` / `[RETRO_TEMPLATE_ID]` / `[RETRO_SESSION_STARTER_ID]`

**Affected files (SPQR template repo):** `docs/skills/censura-discussion.md` · `quaestor-relatio.md` · `ticket-comment.md` (UPDATE) · `docs/retro/retrospector.md` · `input.md` · `output.md` · `discussion.md` · `session-starter.md` (NEW) · `README.md` (UPDATE) · `docs/CONFIGURE.md` (UPDATE).

### Skill-file edits — mirror the Foodoire v1.3 edits + substitutions

These files already exist in the template (from v1.2) — **apply the same edits, do not recreate**:

- `docs/skills/censura-discussion.md` — the **CONVERGENCE stop-branch** (co-located, before-verdict, 4-step, resume "go" / "engage").
- `docs/skills/quaestor-relatio.md` — **both** v1.3 edits: the external-claim **verify** block (research mandate; ACCEPTED / REFUTED / UNKNOWN) **and** the **FETCH STRATEGY** (after the PRE-FLIGHT / Load-in-order block; the 2 model-agnostic rules).
- `docs/skills/ticket-comment.md` — the **session_id** field (FORMAT + NEVER omit + unset → unknown).

Substitute any real names per the rules above.

### `docs/retro/*` — NEW (generic versions)

- Create the 5 retro files mirroring the Foodoire versions, with substitutions: persona names → `[Name 1–4]`; the 3 retro Notion IDs → placeholders; any project-specific content → `[PROJECT_BOUNDARIES]`.
- `**session-starters.md**`** gets NO retro block** — folder-pipeline convention, consistent with Foodoire.

### `README.md` — UPDATE (SPQR repo)

- Add a **Retrospector** row to the agent table; add **RETROACTIO** to the pipeline section.

### `docs/CONFIGURE.md` — UPDATE

- Document the **3 new placeholders** (`[RETRO_PARENT_ID]` / `[RETRO_TEMPLATE_ID]` / `[RETRO_SESSION_STARTER_ID]`) — each with its file location and the value-to-fill (the Foodoire Retroactio parent / template / session-starter Notion IDs as the example value).

### Verification — mandatory before close

- `grep` the synced files for **hardcoded persona / real names** → must be **zero**.
- `grep` for **hardcoded Notion IDs / URLs** → must be **zero** (all replaced by placeholders).
- Report both grep results in Changes Made.

**Rules:**

- **READ_CURRENT_STATE** — the template skill files exist; apply edits, don't recreate.
- Follow `.claude/rules/AGENT_LAWS.md`. **Owner-only commit** — no `git commit` / `git push`.
- **Do NOT touch **`**docs/upgrade/***`** (the upgrade pipeline / CONFIG)** — out of scope; that pipeline runs from runtime-resolved CONFIG and is never customized.

### Changes Made (Group 4)

*Completed. Ran in the SPQR template repo; source of truth = Foodoire v1.3-edited files, replicated generically with substitutions.* **session_id:** `cac78a5d-3969-43f1-ba2f-72838f6557b1`

**Skill-file edits (UPDATE — existing files, mirrored Foodoire v1.3 edits):**

- `docs/skills/censura-discussion.md` — inserted the CONVERGENCE **STOP-BRANCH** (co-located gate, before-verdict, 4 steps, resume on explicit "go"/"engage"). No names present.
- `docs/skills/quaestor-relatio.md` — both v1.3 edits: **FETCH STRATEGY** block (2 rules) added right after PRE-FLIGHT; **verify-external-claims** block (ACCEPTED/REFUTED/UNKNOWN) added in RESEARCH. iOS-specific examples genericized (see Substitution notes).
- `docs/skills/ticket-comment.md` — added **session_id** field in FORMAT, the FIELD RULES line (echo $CLAUDE_CODE_SESSION_ID; unset → `unknown`; never drop), and the `Never omit session_id` constraint.

**docs/retro/* (NEW — 5 generic files):**

- `retrospector.md` — faithful copy; no names/IDs/project terms (EXPLORACIO spelling kept to match template).
- `input.md` — copy; stripped project-instance ticket ref `DOC-014` → `(token cost)`.
- `output.md` — copy; substituted the two hardcoded Notion URLs → `[RETRO_TEMPLATE_ID]` / `[RETRO_PARENT_ID]`; dropped `(Foodoire)` labels and the `DOC-016` ref.
- `discussion.md` — faithful copy; generic.
- `session-starter.md` — project path → `[PROJECT_PATH]`; Laws path made repo-relative (`.claude/rules/AGENT_LAWS.md`, matching template convention — no new placeholder invented); Notion session-starter reference tagged with `[RETRO_SESSION_STARTER_ID]`.
- `session-starters.md` (agents) — **no retro block added**, per folder-pipeline convention.

[**README.md**](http://readme.md/)** (UPDATE):**

- Added **Retrospector** row to the Agents table (RETROACTIO pipeline).
- Added **RETROACTIO** pipeline to the pipelines section ("Two pipelines" → "Three pipelines").
- Added `docs/retro/` block to the file-structure tree (keeps the tree current after adding 5 files).

**docs/**[**CONFIGURE.md**](http://configure.md/)** (UPDATE):**

- Added 3 placeholder rows to the Variable Catalogue with file location + Foodoire example value: `[RETRO_PARENT_ID]` (`37268d5de1e8808091b6f9c3cd66b648`), `[RETRO_TEMPLATE_ID]` (`37268d5de1e8819cada8f75817cb790b`), `[RETRO_SESSION_STARTER_ID]` (`37268d5de1e881ae9822f3b82755d7f8`) — all in `docs/retro/`.
- Added `docs/retro/session-starter.md` to the existing `[PROJECT_PATH]` row's file list.

**Substitution notes (judgment calls, flagged per Law 4):**

- Inline iOS examples in [quaestor-relatio.md](http://quaestor-relatio.md/) were genericized in prose (WWDC/Swift Evolution/App Store → "platform release outcomes / library/framework guarantees / official spec or standards proposal / vendor-gated URLs") rather than replaced with a `[PROJECT_BOUNDARIES]` token — matches the v1.2 precedent (existing template skill files carry generic wording, not inline tokens; `[PROJECT_BOUNDARIES]` is reserved in [CONFIGURE.md](http://configure.md/) for the ticket-slicing whole-section placeholder).
- Project-instance ticket refs (`DOC-014`/`DOC-016`) stripped — meaningless in the generic template.

**Verification (mandatory) — grep over the synced skill + retro files + README:**

- **Hardcoded persona / real names → ZERO.** No real names anywhere; the only persona tokens are the intact `[Name 1–3]` placeholders in [censura-discussion.md](http://censura-discussion.md/) and the descriptive phrase "Single persona" in [retrospector.md](http://retrospector.md/).
- **Hardcoded Notion IDs / URLs → ZERO** in the synced generic files (`notion.so` / 32-hex / UUID patterns: no matches). The 3 example IDs live only in [CONFIGURE.md](http://configure.md/), which documents them by design.
- Project-term scan: the only hits (`swiftlint`/`xcodebuild` at [README.md](http://readme.md/) L40, L214) are **pre-existing v1.1/v1.2 content**, not introduced by Group 4 — flagged for owner as separate generic-template residue, out of this sync's scope.

**Rules honored:** READ_CURRENT_STATE (skill files edited, not recreated); AGENT_[LAWS.md](http://laws.md/); owner-only commit (no `git commit`/`git push` run); `docs/upgrade/*` untouched.