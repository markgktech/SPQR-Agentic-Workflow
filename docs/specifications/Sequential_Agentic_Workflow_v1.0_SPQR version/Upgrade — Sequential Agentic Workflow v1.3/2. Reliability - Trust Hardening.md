---

---
Close two correctness gaps surfaced in SPIKE-007. **Foodoire-first → SPQR sync.**

---

## DOC-15 — Censura CONVERGENCE stop-branch

[Ticket](https://www.notion.so/37168d5de1e881b4aba3db4c7629754a)

**Decided direction:** fix the **one** gate — a co-located stop-branch, **evaluated before verdict commitment** (not just a documented output format); altitude-gate-consistent. Bigger arc (gate primitive + pipeline gate audit) → [gate-semantics SAW](https://www.notion.so/37368d5de1e881abb068ea5b078a44fe).

## DOC-16 — Quaestor verify external claims

[Ticket](https://www.notion.so/37168d5de1e881c7bc4eeb6e3ceabaa0)

**Decided direction:** verify instruction in the research mandate; **ACCEPTED / REFUTED / UNKNOWN**; scope tight to decision-bearing external claims. Bigger arc (per-claim provenance across handoffs) → [SAW-10](https://www.notion.so/37268d5de1e8817b9cbbd7ebe41fed52).

---

## Execution brief — Reliability / Trust Hardening (Group 2)

*Self-contained (v1.2 form) — the execution agent can act from this page alone.*

**Metadata**

- **GROUP:** Reliability / Trust Hardening · **ORDER:** Foodoire, position flexible (collision-free — distinct files); SPQR-sync last (finalize N/N when all briefs exist)
- **REPO:** Foodoire (SPQR placeholder versions → sync group)
- **RATIONALE:** two small correctness fixes to distinct skill files; no shared files
- **READ_CURRENT_STATE:** yes — read each file before editing · **FILL_CHANGES_MADE:** yes

**PRE_FLIGHT (load these):**

- DOC-15 ticket + DOC-16 ticket (background)
- `censura-discussion.md` — read the current CONVERGENCE section (anchor for co-location)
- `quaestor-relatio.md` — read the research-mandate / pre-flight section

**Affected files (Foodoire):** `docs/skills/censura-discussion.md` (UPDATE) · `docs/skills/quaestor-relatio.md` (UPDATE). Distinct files — no collision.

### `docs/skills/censura-discussion.md` — UPDATE (DOC-15)

**What:** Add the CONVERGENCE **stop-branch**, **co-located** with the trigger condition ("Owner check-in required if: personas disagree on a FAIL, **or significant emergent gap found**"), AND **position it as a sequenced gate BEFORE the OUTPUT/verdict step** — structural, not a retrospective annotation after the verdict (the deeper SPIKE-007 factors were *early verdict commitment* + *skill-file-as-template*). **Stop format (4 steps):** (1) stop before writing the CONVERGENCE summary or any output block; (2) present each emergent gap with its finding tag (RISK/NOTE) + one sentence; (3) state "Owner check-in required — waiting for explicit closure before proceeding to output"; (4) wait. **Resume:** after the owner explicitly closes with an **affirmative ("go" / "engage")**, proceed to the OUTPUT step (verdict stands, now carrying the acknowledged gaps). Trigger and stop-branch must be **co-located** — reading the condition immediately shows the expected stop behaviour.

**Where:** `censura-discussion.md` — CONVERGENCE section.

### `docs/skills/quaestor-relatio.md` — UPDATE (DOC-16)

**What:** In the **research-mandate section** (confirm it exists; add if not): Consilium claims about external facts (API behaviour, bug fixes, WWDC outcomes, library guarantees) must be **independently verified during research** — never inherited. **Action:** search for a primary source (release notes / Swift Evolution proposal / WWDC session ID). **Three outcomes:** **ACCEPTED** = primary source found + cited; **REFUTED** = primary source contradicts the claim → flag as a finding (the Consilium claim is *wrong*, not merely unverified); **UNKNOWN** = no source either way → escalate (never silently inherited). **Scope:** decision-bearing external claims only — a claim a recommendation/decision rests on; skip incidental mentions. (Consistent with DOC-14's "external URLs only" fetch rule — no conflict.)

**Where:** `quaestor-relatio.md` — research-mandate / pre-flight section.

**Out of scope here:** generalizable arcs — DOC-15 gate primitive → gate-semantics SAW · DOC-16 per-claim provenance across *all* handoffs → SAW-10.

### Changes Made (Group 2)

**Status:** Done — both UPDATEs applied to Foodoire. SPQR placeholder sync deferred to the sync group (out of scope here). No commit/push (owner-only). [CLAUDE.md](http://claude.md/) untouched.

`**docs/skills/censura-discussion.md**`** (DOC-15):** Added a `STOP-BRANCH` block inside the CONVERGENCE section, co-located immediately after the existing trigger line ("Owner check-in required if: ... or significant emergent gap found"). Framed structurally as a gate evaluated *before* committing the verdict / writing any output (explicitly "not a retrospective annotation"), directly addressing the SPIKE-007 early-verdict-commitment + skill-file-as-template root causes. 4-step stop format (stop before any output → present each gap with RISK/NOTE tag + one sentence → state "Owner check-in required — waiting for explicit closure before proceeding to output" → wait). Resume only on owner affirmative ("go" / "engage"), then proceed to OUTPUT with verdict carrying the acknowledged gaps. Existing structure/order preserved; the autonomous-close line was kept directly after the new block.

`**docs/skills/quaestor-relatio.md**`** (DOC-16):** Added a "Verify external claims — never inherit" instruction in the RESEARCH (research-mandate) section, after the per-chunk investigate line and before the Consilium-decisions compare step. Scope tightened to decision-bearing external claims only (skip incidental mentions). Three outcomes defined: ACCEPTED (primary source found + cited), REFUTED (source contradicts → flag as a finding; claim is wrong, not merely unverified), UNKNOWN (no source → escalate, never silently inherit). Primary-source examples carried over (release notes / Swift Evolution proposal / WWDC session ID). No conflict with DOC-14's external-URL fetch rule. Existing structure/order preserved.

**session_id:** `4f3d6418-18f1-494f-abbd-363d02068895`