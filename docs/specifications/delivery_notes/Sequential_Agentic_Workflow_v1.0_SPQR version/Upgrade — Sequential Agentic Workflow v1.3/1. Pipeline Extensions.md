---

---
New capabilities — the first new agent since the core pipeline (Retrospector). Lands **Foodoire-first → SPQR sync** (no SPQR-only items, except README).

---

## DOC-12 — Idea intake — ⏸ DEFERRED (not in v1.3)

[Ticket](https://www.notion.so/37168d5de1e881d48b82f0d94cbc6d5b)

> **Deferred to a later batch** — DOC-12's create-step depends on SAW-2 (routing) + SAW-8 (templates/naming), a separate process-transition (SAW-8's numbering decision is still open). Decided direction is **preserved** for that batch (light separate-session triage; sense-making → dedup → fold-in core; routing/templates inherited from SAW-2/SAW-8; 3-lane type handling: SAW/DOC/SPIKE create · DEV/BUG classify+flag · discard). Not wasted.

---

## DOC-18 — RETROACTIO

[Ticket](https://www.notion.so/37268d5de1e8811da918d82c92b25d02)

**Decided direction:** in-repo retro pipeline, **both repos** (Foodoire-first → SPQR sync) · git inspection via `--stat` + working tree (marker-based, **not** commit messages) · **rule-rot pass** in scope · base = qualitative record signals, **quantitative telemetry OUT** (later rung) · the **clean seed of the future self-building governance loop** ([north-star SAW](https://www.notion.so/37368d5de1e8815caf04d23cda6bf69c)) — keep it small now.

---

## Execution brief — RETROACTIO (Group 1a)

*Self-contained (v1.2 form) — the execution agent can act from this page alone.*

**Metadata**

- **GROUP:** RETROACTIO · **ORDER:** Foodoire, position flexible (independent new files); SPQR-sync last (finalize N/N when all briefs exist)
- **REPO:** Foodoire (SPQR placeholder version + README → SPQR-sync group)
- **RATIONALE:** one cohesive new pipeline — all `docs/retro/*` files belong together
- **READ_CURRENT_STATE:** yes — read each file before editing · **FILL_CHANGES_MADE:** yes

**PRE_FLIGHT (load these):**

- DOC-18 ticket (background)
- `consilium-discussion.md` — [discussion.md](http://discussion.md/) mirrors its HITL pattern
- `ticket-comment.md` — [output.md](http://output.md/) must state it does NOT apply
- Retrospective **parent** (create each retro as a child page here): [[Retroactio]]
- **TEMPLATE — Retrospective** (mirror sections EXACTLY): [[TEMPLATE — Retrospective]]
- existing **Session starter — Retro agent** (mirror for `session-starter.md`): [[Session starter — Retro agent]]
- ⚠ all three Notion IDs are **PROJECT-SPECIFIC** → in the SPQR repo they become placeholders (`[RETRO_PARENT_ID]` / `[RETRO_TEMPLATE_ID]` / `[RETRO_SESSION_STARTER_ID]`) in [CONFIGURE.md](http://configure.md/); the sync grep verifies zero hardcoded matches.

**Affected files (Foodoire):** `docs/retro/retrospector.md` (NEW) · `input.md` (NEW) · `output.md` (NEW) · `discussion.md` (NEW) · `session-starter.md` (NEW). [README.md](http://readme.md/) → SPQR-sync group (not here). `**docs/agents/session-starters.md**`** is NOT modified — see the correction below.**

### `docs/retro/retrospector.md` — NEW

**What:** Agent definition for RETROACTIO — a **single-agent, single-session** pipeline (no handoff chain). **Triggers (two, same agent):** (1) **milestone** — owner-driven at meaningful milestones (pipeline completion, first dev tickets, first shipped feature); (2) [**LESSONS.md**](http://lessons.md/)** counter** — Censura's 10-entry counter *signals* but does NOT auto-trigger. **Reads:** per [input.md](http://input.md/). **Produces:** a Notion child page (per [output.md](http://output.md/)). **Explicit:** does NOT follow `ticket-comment.md` — output is a Notion child page, not a ticket comment. References the Laws.

**Where:** new file.

### `docs/retro/input.md` — NEW (preflight)

**What:** Load order — (1) [LESSONS.md](http://lessons.md/) → (2) previous retro Notion page (skip if first run) → (3) Censura comments from the tickets in scope (NOT full ticket bodies) → (4) **repo state**: `git diff --stat <marker>..HEAD` **plus** uncommitted working tree (`git status` / `git diff --stat`), marker-based; **NOT** commit messages/cadence (owner-only commit → reflects habit, not work structure); no full diffs by default. session_id via `echo $CLAUDE_CODE_SESSION_ID`. **Boundary:** metrics here = qualitative record signals (LESSONS, Censura, git churn); **quantitative telemetry is OUT** (later rung) — do not over-build.

**Where:** new file.

### `docs/retro/output.md` — NEW (output protocol)

**What:** Fetch the **TEMPLATE — Retrospective** and follow it **EXACTLY** — same sections, same order, no additions/removals. Create the retro as a Notion **child page under the Retrospective parent**. Write **session_id** as page metadata. **Max 3 action items.** **No DOC/SPIKE/SAW ticket creation — flag candidates only.** Include the **rule-rot pass**: flag pipeline rules/skills that no longer fire (never caught anything recently) as removal candidates — **evidence-based** (from the LESSONS/Censura record, not model self-report); flag only, owner removes. **Record the closing marker** for the next retro (defines [input.md](http://input.md/)'s `<marker>`).

**Where:** new file.

### `docs/retro/discussion.md` — NEW (HITL gate)

**What:** Mandatory deliberation between input and output (mirror `consilium-discussion.md`): (1) agent presents findings — observations only, no output block yet; (2) owner shares their own observations; (3) bidirectional — agent may give feedback to owner on observed process behaviour; (4) owner explicitly closes with an **affirmative ("go" / "engage")** before output proceeds. HITL gate is **unconditional** — never proceed to output without explicit owner closure ("let's move on" ≠ closure, Law 2).

**Where:** new file.

### `docs/retro/session-starter.md` — NEW

**What:** Session starter for RETROACTIO. **Mirror the existing Notion "Session starter — Retro agent" page** (don't reinvent). References [input.md](http://input.md/) + [output.md](http://output.md/). Includes a **milestone field** + a **ticket-URL list** as owner-supplied input. **This is RETROACTIO's only session-starter** (folder-pipeline convention).

**Where:** new file.

### ⚠ Correction (v1.3 session) — `docs/agents/session-starters.md` is NOT in this group

The RETROACTIO session-starter lives **only** in `docs/retro/session-starter.md` (folder-pipeline convention — same as `docs/upgrade/`). `docs/agents/session-starters.md` is the shared session-starter for the **agent-modes** (OPUS / EXPLORATIO); adding a RETROACTIO block there **duplicates** the folder file. **If the first execution already appended a RETROACTIO block to **`**session-starters.md**`**, remove it.**

**Out of scope here:** [README.md](http://readme.md/) (Retrospector row + RETROACTIO pipeline entry) → **SPQR-sync group** (SPQR-repo only, per v1.2). Generalizable rule-rot / gate arcs → their SAWs.

### Changes Made (DOC-18)

*Filled in at close by the execution agent — file by file.*